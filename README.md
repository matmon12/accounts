# Учетные записи

Веб-приложение для управления учетными записями, реализованное в рамках тестового задания для Vue Frontend Developer.

## 🚀 Технологии

- **Vue 3.5** + Composition API
- **TypeScript 5.8**
- **Pinia 3.0** — управление состоянием с сохранением в localStorage
- **PrimeVue 4.5** — UI библиотека
- **Vite 7** — сборщик

## ✨ Реализованный функционал

### Форма управления учетными записями

- ✅ **Добавление записей** — кнопка "Добавить" создает новую пустую запись
- ✅ **Удаление записей** — кнопка удаления полностью удаляет запись из списка и localStorage
- ✅ **Валидация полей** — происходит при потере фокуса (для текстовых полей) и при изменении значения (для селекта)
- ✅ **Визуальная индикация ошибок** — невалидные поля выделяются красной обводкой
- ✅ **Сохранение в localStorage** — все записи сохраняются автоматически при валидации
- ✅ **Загрузка при старте** — сохраненные записи загружаются при обновлении страницы

### Поля учетной записи

1. **Метка** — необязательное поле, максимум 50 символов, несколько меток через `;`
2. **Тип записи** — выпадающий список (LDAP / Локальная)
3. **Логин** — обязательное поле, максимум 100 символов
4. **Пароль** — обязательное для типа "Локальная", максимум 100 символов, скрывается для LDAP
5. **Кнопка удаления** — удаляет запись

### Валидация

- **Логин**: обязательное поле, не может быть пустым, максимум 100 символов
- **Пароль**: обязательное для типа "Локальная", не может быть пустым, максимум 100 символов
- **Метка**: необязательное поле, максимум 50 символов


## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
```

## 📁 Структура проекта

```
src/
├── views/
│   └── HomePage.vue          # Главная страница с формой
├── stores/
│   └── accounts.ts           # Pinia store для управления записями
├── types/
│   └── account.ts            # TypeScript типы
└── main.ts                   # Точка входа с настройкой PrimeVue
```

## 🎨 UI компоненты

Используются компоненты из PrimeVue:

- `Button` — кнопки добавления и удаления
- `InputText` — поля ввода метки и логина
- `Select` — выбор типа записи
- `Password` — поле ввода пароля с возможностью показа/скрытия
